{
  "name": "month-planner-root",
  "private": true,
  "scripts": {
    "dev:web": "npm run dev --prefix web",
    "dev:api": "npm run dev --prefix api",
    "install:all": "npm install --prefix web && npm install --prefix api"
  }
}
